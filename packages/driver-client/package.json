{
  "name": "@shiftx/driver-client",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Driver-first client helpers for ShiftX",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "demo": "cross-env FIRESTORE_EMULATOR_HOST=127.0.0.1:8081 FUNCTIONS_EMULATOR_HOST=127.0.0.1:5002 ts-node src/demoDriverFlow.ts",
    "dev:emulators": "cd ../.. && firebase emulators:start --only functions,firestore,auth"
  },
  "dependencies": {},
  "peerDependencies": {
    "firebase": "^11.10.0"
  },
  "devDependencies": {
    "firebase": "^11.10.0",
    "cross-env": "^7.0.3",
    "firebase-admin": "^11.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3"
  }
}
