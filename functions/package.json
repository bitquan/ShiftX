{
  "name": "functions",
  "description": "Cloud Functions for shiftx",
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext .ts src",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "eslint-config-google": "^0.14.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@types/node": "^18.0.0",
    "@types/glob": "^7.2.0",
    "@firebase/rules-unit-testing": "^2.0.0",
    "mocha": "^10.2.0",
    "chai": "^4.3.8",
    "ts-node": "^10.9.1",
    "@types/mocha": "^10.0.1",
    "@types/chai": "^4.3.0"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext .ts src",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test:rules": "mocha -r ts-node/register 'test/**/*.test.ts' --exit",
    "test:functions": "mocha -r ts-node/register 'test/**/*.unit.test.ts' 'test/driver.unit.test.ts' --exit"
  },
  "overrides": {
    "minimatch": "3.0.6",
    "glob": "7.2.3"
  }
}
